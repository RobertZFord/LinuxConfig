super + Return
  alacritty

super + Escape
  pkill -USR1 -x sxhkd

super + p
  rofi -terminal alacritty -show drun

super + w
  rofi -show window

shift + super + {q,r}
  bspc {quit,wm -r}

## Super + # switches to one of the named desktops
super + {1,2,3,4,5,6,7,8,9,0}
  bspc desktop {Alpha,Bravo,Charlie,Delta,Echo,Foxtrot,Golf,Hotel,India,Juliett} --focus

#shift + super + {1,2,3,4,5,6,7,8,9,0}
#  bspc node --to-desktop {Alpha,Bravo,Charlie,Delta,Echo,Foxtrot,Golf,Hotel,India,Juliett}

#control + super + {1,2,3,4,5,6,7,8,9,0}
#  bspc node --to-desktop {Alpha,Bravo,Charlie,Delta,Echo,Foxtrot,Golf,Hotel,India,Juliett} --follow

## Move to the previous or next desktop
#super + {Up, Down}
#  bspc desktop {prev, next} --focus

## Move focus to the previous or next window on this desktop
super + {Left,Right}
  bspc node --focus {prev, next}.local

## Swap focused with with the previous or next window on this desktop
control + super + {Left, Right}
  bspc node --swap {prev, next}.local

## Move the focused window to the previous or next desktop and maintain focus
#control + super + {Left,Right}
#  bspc node --to-desktop {prev,next} --follow

#super + q ; m; {1,2,3,4,5,6,7,8,9,0}
super + m; {1,2,3,4,5,6,7,8,9,0}
  bspc node --to-desktop {Alpha,Bravo,Charlie,Delta,Echo,Foxtrot,Golf,Hotel,India,Juliett}

super + x ; super + x ; super + x
  bspc node --kill

# F6 and F7 have the brightness down/up icon on my chromebook keyboard
super + {F6,F7}
  light -{U,A} 20

